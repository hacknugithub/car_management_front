This is a fullstack excercise with an api built in ruby and ruby on rails
While the frontend is built in React JS

You can clone both the repos from here and each one has instructions to run and develop 
